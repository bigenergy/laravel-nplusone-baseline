<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <!-- The extension is deliberately NOT registered here. It is exercised by
         tests/Fixtures/Suite, which ReportIsWrittenTest runs as a child
         process; registering it for this suite as well would have it rewriting
         the report of whichever test booted an application last. -->

    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
